/**
 * Local development config — copy this file to config.js and add your MapTiler key.
 * config.js is gitignored; production builds inject it via GitHub Actions.
 */
window.APP_CONFIG = {
  MAPTILER_KEY: 'your-maptiler-key-here',
};
